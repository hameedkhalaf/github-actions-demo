name: Repository Despatch
on:
    repository_dispatch:
        types: [test]

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v3
          - run: echo ${{ github.event.client_payload.sha }}
          - run: echo "Success"